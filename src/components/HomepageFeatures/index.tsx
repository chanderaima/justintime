// DEPRECATED: HomepageFeatures component removed from the page. Kept as an archival stub.
// If you want this feature block back, restore the original markup from version history or contact the maintainer.

import type {ReactNode} from 'react';

export default function HomepageFeatures(): ReactNode {
  return null;
}
